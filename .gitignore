/build/
# Built apk repo output (regenerated by build-apk.sh / CI).
/dist/
/site/
# Private package-signing key; the public half (keys/retasker.rsa.pub) is committed.
keys/retasker.rsa
# Holds the OpenRouter API key; deployed to the device, never committed.
src/viewer/config.json
# Desktop design emulator: local dev tooling, not part of the shipped app.
/emulator/
# Installed deps for tools/ (record-stream.mjs); regenerated by npm install.
/tools/node_modules/
